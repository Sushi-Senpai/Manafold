# Server-side only (read by next.config.ts's rewrites(), never exposed to
# client JS) — the Go backend's real origin that /api/* is proxied to. The
# browser never sees this value; it only ever calls this frontend's own
# /api/* paths.
BACKEND_ORIGIN=http://localhost:8080
